none            `
checkmd5        `checkmd5
checkfs         `checkfs
toram           `toram
from=usb        `from=usb
automount       `automount
mount=usb       `mount=usb
mount=all       `mount=all
nousb2          `nousb2
acpi=off        `acpi=off
hwclock=utc     `hwclock=utc
hwclock=local   `hwclock=local
